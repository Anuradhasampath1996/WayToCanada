# Prebuilt government-form PDF processor (iText pdfXFA append JAR) + XFA fixtures.
# Copied into the API image at /var/www/form-processor/
# Rebuild the JAR locally with:
#   mvn -f form-processor-poc/java-itext/pom.xml -DskipTests package
# then copy target/government-form-poc-itext-0.1.0-SNAPSHOT.jar into this folder.
